---
import { WORK_TYPE_LABELS, type WorkType } from '../lib/filterJobs';

interface Props {
  workType: WorkType;
}

const { workType } = Astro.props;
---

<span class="rounded-full bg-accent-50 px-2.5 py-0.5 text-xs font-medium text-accent-700">
  {WORK_TYPE_LABELS[workType]}
</span>
